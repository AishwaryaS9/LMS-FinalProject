<!Doctype html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Learner Wise</title>
    <style>
        @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;400;600&display=swap");

        :root {
            /* --blue: #ff4757; */
            --blue: #00bbf0;
            --main-color: #e8786e;
            --black: #555;
            --light-color: #777;
            --bg: #f8f8fa;
            --border: 0.1rem solid rgba(0, 0, 0, 0.1);
            --box-shadow: 0 0.5rem 1.5rem rgba(0, 0, 0, 0.1);
        }

        * {
            font-family: "Poppins", sans-serif;
            margin: 0;
            padding: 0;
            outline: none;
            border: none;
            box-sizing: border-box;
            text-decoration: none;
            transition: all 0.2s linear;
            font-weight: normal;
            text-transform: capitalize;
            scroll-behavior: smooth;
        }

        *::selection {
            background: var(--blue);
            color: #fff;
        }

        html {
            font-size: 62.5%;
            overflow-x: hidden;
        }

        section {
            min-height: 100vh;
            padding: 0 9%;
            padding-top: 7.5rem;
            padding-bottom: 3rem;
        }

        button {
            font-family: inherit;
            user-select: none;
        }

        .btn {
            display: inline-block;
            border-radius: 5rem;
            background: #444;
            color: #fff;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.2);
            padding: 0.7rem 3rem;
            position: relative;
            z-index: 0;
            overflow: hidden;
            font-size: 1.7rem;
        }

        .btn::before,
        .btn::after {
            content: "";
            position: absolute;
            top: 0;
            height: 100%;
            width: 0%;
            background: var(--blue);
            z-index: -1;
            transition: 0.3s linear;
        }

        .btn::before {
            left: 0;
        }

        .btn::after {
            right: 0;
        }

        .btn:hover::before,
        .btn:hover::after {
            width: 100%;
        }


        .heading {
            text-align: center;
            padding: 1rem;
            color: var(--blue);
            font-size: 6rem;
            -webkit-text-stroke: rgb(31, 154, 211) 0.1rem;
            text-shadow: 0 0.3rem 0.5rem rgba(63, 152, 253, 0.1);
            font-weight: bolder;
            text-transform: uppercase;
            letter-spacing: 0.3rem;
        }


        header {
            position: fixed;
            top: 0;
            left: 0;
            z-index: 1000;
            width: 100%;
            background: #fff;
            padding: 2rem 5%;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        header .navbar a {
            font-size: 1.7rem;
            margin-left: 2rem;
            color: #444;
        }

        header .navbar img {
            width: 16rem;
            height: 6rem;
        }

        header .navbar .navbarlogo {
            margin-right: 65rem;
        }

        header .navbar a:hover {
            color: var(--blue);
        }

        header .scroll-bar {
            position: absolute;
            top: 100%;
            left: 0;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
            height: 0.8rem;
            background: var(--blue);
            width: 0;
        }

        .task-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background-image: linear-gradient(to right, #f6f9ff, #dff2ff, #bfedff, #96e9ff, #62e5fc);
            position: relative;
            overflow: hidden;
            z-index: 0;
            padding-bottom: 90px;
            margin-bottom: 2px;
            margin-top: 5rem;
        }

        .task-container .row {
            display: flex;
            flex-wrap: wrap;
        }

        .task-container .row .column {
            flex: 1 1 40rem;
            margin: 1rem;
            padding: 2rem;
            border: 0.1rem solid rgba(0, 0, 0, 0.2);
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
            border-radius: 0.5rem;
        }

        .task-container .row .column .box {
            padding-left: 2.5rem;
            padding-bottom: 2rem;
            border-left: 0.2rem solid var(--blue);
            position: relative;
        }

        .task-container .row .column .label-checkbox {
            font-size: 16px;
            line-height: 5px;
            background: #fff;
            border-radius: 12px;
            padding-left: 3rem;
            margin-bottom: 2rem;
            height: 30px;
        }

        .task-container .row .column .checkbox-style {
            margin-top: 2rem;
            margin-bottom: 1rem;
            align-items: left;
            margin-left: 0;
        }

        .center {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 35px;
            padding-top: 3rem;
            padding-bottom: 5rem;

        }

        .center .btn {
            padding-left: 2rem;
            margin-right: 2rem;
        }
    </style>
</head>

<body>
    <header>
        <nav class="navbar">
            <a class="navbarlogo" href="/userprofile">
                <img src="/images/logo.png">
            </a>
            <a aria-current="page" href="/">Home</a>
            <a href="/userprofile">Profile</a>
            <a href="/task">Add task</a>
            <a href="/course">your courses</a>
            <a href="/logout">Logout</a>
            <div class="scroll-bar"></div>
        </nav>

    </header>

    <div class="task-container">
        <div class="row">
            <div class="column">
                <div class="box">
                    <ul>
                        <% Tasks.forEach(details=> { %>
                        <% if (details._id==idTask) { %>
                        <li>
                            <form action="/task/edit/<%= details._id %>" method="POST">
                                <input type="text" style="width: 60%;" class="label-checkbox"
                                    value="<%= details.content %>" name="content">
                                <input type="checkbox" value="<%= details.iscomplete %>" name="comp"
                                    class="checkbox-style">
                                <label style="font-size: 14px;">Change Status</label>
                                <div class="center">
                                    <button type="submit" class="btn">Confirm</button>
                                    <button type="submit" class="btn" window.location.href='/' ;>Cancel</button>
                                </div>
                            </form>
                        </li>
                        <% } else { %>

                        <% } %>
                        <% }) %>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-U1DAWAznBHeqEIlVSCgzq+c9gqGAJn5c/t99JyeKa9xxaYpSvHU5awsuZVVFIhvj" crossorigin="anonymous">
    </script>

</body>

</html>